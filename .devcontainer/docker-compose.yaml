version: "3"
services:
  hugo:
    build: hugo
    environment:
      LANG: ja_JP.utf8
    volumes:
      - $HOME/.ssh:/root/.ssh
      - ..:/workspace
      - $HOME/.gitconfig:/root/.gitconfig
    tty: true
